name: Daily real estate refresh

on:
  schedule:
    - cron: "15 2 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-real-estate-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Check out repository
        uses: actions/checkout@v7.0.1

      - name: Set up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.14"
          cache: pip
          cache-dependency-path: requirements-dev.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt
          python -m playwright install --with-deps chromium

      - name: Configure Git author
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Refresh listings
        env:
          PYTHONUNBUFFERED: "1"
        run: >
          python -m reality.refresh_real_estate_ads
          --local-only
          --push
          --commit-message "Refresh real estate ads"

      - name: Validate real estate data
        run: python -m reality.validate_real_estate_data --summary-output real_estate_refresh_summary.md

      - name: Upload refresh summary
        if: always()
        uses: actions/upload-artifact@v7.0.1
        with:
          name: real-estate-refresh-summary
          path: real_estate_refresh_summary.md
          if-no-files-found: ignore
